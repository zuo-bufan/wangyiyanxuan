module.exports = {
    // publicPath: process.env.NODE_ENV === 'production'? '/网易严选/': '/',
    assetsDir: 'static',
    parallel: false,
    publicPath: './',
    devServer:{
        port:'80',
        open:true
    }
}

